#¿ Local search algorithms used in this work
local_search_algorithms <- list(
  fpls = "frontier pareto local search",
  pls = "pareto local search",
  nmols = "narrow multiobjective local search", 
  lmols = "large multiobjective local search", 
  mosa = "multiobjective simulated annealing", 
  pr = "path relinking", 
  ensemble = "clustering ensemble"
)

#' Biological databases used in this work
biological_databases <- list(
  go = "gene ontology",
  string = "STRING",
  kegg = "KEGG-pathway",
  mesh = "MeSH",
  do = "disease",
  disgenet = "disgenet"
)